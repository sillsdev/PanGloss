# STAGING: late-structural-anchor-five-rule-chain

## Purpose

This generic synthetic fixture is derived from the shape of a private Mbugwe failure, without copying language data. Four ordinary prefixes precede a discontinuous fifth rule in an unordered stratum. `fedcbag` is load-bearing: it requires the full five-rule chain and the late structural anchor; bare `a` is the negative control.

## Oracle and RED evidence

The oracle is PanGloss `pg_parse::Morpher`, not the founding C# implementation. The full engine generated `fedcbag` in the earlier experiment. At `f56a598e`, the Foma structural fallback is bounded to three extra rules, so this fixture is intended to remain a clear RED conformance artifact until a sound depth policy is implemented.

## Graduation

Not yet proposed upstream. Candidate destination: `machine/conformance/edge-cases/late-structural-anchor-five-rule-chain/`.
