# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

# VENDORED PATCH (PanGloss, see VENDORED.md): empty [workspace] table so this vendored
# copy is self-contained instead of being claimed by the enclosing hc workspace.
[workspace]

[package]
edition = "2024"
rust-version = "1.85"
name = "foma"
version = "0.1.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Idiomatic Rust port of foma, a finite-state compiler and library for constructing and applying finite-state automata and transducers (original C foma by Mans Hulden, Apache-2.0)."
readme = "README.md"
keywords = [
    "finite-state",
    "transducer",
    "fst",
    "nlp",
    "morphology",
]
categories = [
    "science",
    "text-processing",
    "command-line-utilities",
]
license = "Apache-2.0"
repository = "https://github.com/divvun/foma-rs"

[package.metadata.docs.rs]
all-features = true

[lib]
name = "foma"
path = "src/lib.rs"

[[bin]]
name = "cgflookup"
path = "src/bin/cgflookup.rs"

[[bin]]
name = "flookup"
path = "src/bin/flookup.rs"

[[bin]]
name = "foma"
path = "src/bin/foma.rs"

[[test]]
name = "cli"
path = "tests/cli.rs"

[[test]]
name = "cli_slice1"
path = "tests/cli_slice1.rs"

[[test]]
name = "upstream"
path = "tests/upstream.rs"

[dependencies.flate2]
version = "1"

[dependencies.nfst-lexc]
version = "0.1.0"

[dependencies.nfst-xre]
version = "0.1.0"

[dependencies.smol_str]
version = "0.3"

[dependencies.tracing]
version = "0.1"

[dependencies.tracing-subscriber]
version = "0.3"
