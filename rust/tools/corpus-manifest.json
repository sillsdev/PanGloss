{
  "$comment": [
    "Declares every PRIVATE corpus input this repository's tests depend on. Contains NO corpus",
    "content -- only names, relative paths, purpose, and which tests require them.",
    "",
    "Why this file exists: the real-language inputs under samples/data/ are intentionally",
    "untracked, and many corpus-dependent tests are #[ignore]d with a skip-if-absent guard. That",
    "combination is a false-success path -- a worktree can report that it ran the corpus suite",
    "while testing no corpus data at all. Observed: a fresh agent worktree has no samples/data/,",
    "so every such test returned success having measured nothing.",
    "",
    "Contract: `pg.ps1 -Mode corpus-test` validates every required path in this manifest BEFORE",
    "cargo starts and refuses to run if any is missing. It also sets PANGLOSS_CORPUS_REQUIRED=1,",
    "which flips the Rust helpers in pg_conformance_fixtures::corpus from skip-if-absent to panic-",
    "if-absent. Ordinary fixture-independent test runs are unaffected.",
    "",
    "Keep `requiring_tests` accurate: it is how a reader finds what breaks when a corpus goes",
    "missing. Every entry is RESOLVED against rust/crates/, not merely shape-checked: a name that",
    "matches no test fails the manifest-validation test. Shape alone was not enough -- aweti",
    "declared a well-formed `compose_recall_aweti_gate` that matched no test in any branch of this",
    "repository's history, so a fail-closed contract named a gate that could never run."
  ],
  "schema_version": 1,
  "corpus_root": "samples/data",
  "corpora": [
    {
      "logical_name": "indonesian",
      "purpose": "Template-less standalone-rule morphotactics with MPR-gated nasal substitution. The reference grammar for proposer recall on the gated compile path, and the only real grammar whose backend candidates currently reach full-HC confirmation.",
      "files": [
        { "path": "indonesian.fwdata", "role": "grammar-fwdata", "required": true },
        {
          "path": "indonesian-words.txt",
          "role": "corpus",
          "required": true,
          "word_list": {
            "line_ending": "CRLF",
            "skip_leading_lines": 0,
            "notes": "No header. Two known non-representative lines documented in docs/fst-plan/corpus-word-list-hazards.md: line 100 'meⁿ' is alphabet-valid (segment char29) but is a bound meN- prefix fragment with no attached root, so it always analyzes as ok/no-parse ('-'); line 119 'write-CONTpijit' is gloss-template contamination (uppercase Latin letters + an English word + a gloss abbreviation) that contains characters outside the char table and is always batch status SKIPPED."
          }
        },
        { "path": "indonesian-realize.toml", "role": "realizer-config", "required": false }
      ],
      "requiring_tests": [
        "pg-foma --test p6_gate_parity indonesian_full_corpus_parity_unregressed",
        "pg-foma --test p6_gate_parity indonesian_mpr_exclusion_matches_oracle",
        "pg-foma --test backend_runtime_net_is_queryable_gate corpus_indonesian_first_word_runtime_phases_complete",
        "pg-foma --test backend_runtime_net_is_queryable_gate corpus_indonesian_registry_candidates_are_named_before_build",
        "pg-foma --test backend_runtime_net_is_queryable_gate corpus_indonesian_plan_composed_baseline_completes",
        "pg-foma --test backend_runtime_net_is_queryable_gate corpus_indonesian_confirms_after_the_finish_step",
        "pg-foma --test five_language_backend_reports_gate indonesian_backend_reports_are_complete"
      ]
    },
    {
      "logical_name": "sena",
      "purpose": "Large-lexicon templated grammar (255k+ lexc lines). Drives emit/compile throughput and the propose-then-confirm perf track.",
      "files": [
        { "path": "sena.fwdata", "role": "grammar-fwdata", "required": true },
        {
          "path": "sena-words.txt",
          "role": "corpus",
          "required": true,
          "word_list": {
            "line_ending": "CRLF",
            "skip_leading_lines": 0,
            "notes": "No header, but the char table (samples/data/sena-hc.xml) defines no hyphen segment or boundary, so all ~73 hyphenated reduplication entries (e.g. 'ampfaka-mpfaca') are guaranteed batch status SKIPPED, not a recall failure. A handful of lines are outright non-word contamination and should be excluded from any recall measurement: 'require' (English), 'neg-2psing'/'neg-3p'/'h-u-dadya'/'h-a-da-dya' (interlinear-gloss/morpheme-break debris), 'v-front' and 'det-suffix' (metalinguistic labels), 'lox2' (stray digit). A few more (e.g. 'fátima', 'subtítulo', 'cabeça', 'secondário') are Portuguese loan/proper-noun spellings using accented characters (á/í/ç) also absent from the char table. Full accounting in docs/fst-plan/corpus-word-list-hazards.md."
          }
        },
        { "path": "sena-worst-words.txt", "role": "pinned-worst-case", "required": false }
      ],
      "requiring_tests": [
        "pg-foma --test f1_large_lexicon_gate",
        "pg-foma --test five_language_backend_reports_gate sena_backend_reports_are_complete"
      ]
    },
    {
      "logical_name": "amharic",
      "purpose": "Templatic root-and-pattern interdigitation with AffixTemplate morphotactics. The grammar whose productive morphology lives in the composite-emission and structural-composite subtrees that build_controllable does not build.",
      "files": [
        { "path": "amharic.fwdata", "role": "grammar-fwdata", "required": true },
        {
          "path": "amharic-words.txt",
          "role": "corpus",
          "required": true,
          "word_list": {
            "line_ending": "CRLF",
            "skip_leading_lines": 4,
            "notes": "Lines 1-4 are an English-gloss header ('break','pay','pfv','want'), not Ethiopic surface words; a naive head -N slice reads these as unanalyzable words and presents as a recall failure (the motivating bug for docs/fst-plan/corpus-word-list-hazards.md). Real words start at line 5. Separately, 17 word lines beyond the header (e.g. lines 63-65, 71-75, 91, 105, 355, 502, 512, 601, 604, 615, 627) use Ethiopic letter variants (ሕ ሐ ሥ ሦ ሃ ዓ ፀ) that are absent from the grammar's CharacterDefinitionTable and are therefore also guaranteed batch status SKIPPED. This corpus is also SLOW: several individual words (e.g. lines 5, 12, 17, 29-32) take multiple seconds to confirm and can make even a 40-word sample exceed a naive process timeout; always pass --word-timeout-ms to `pangloss batch` when sampling it."
          }
        },
        { "path": "amharic-worst-words.txt", "role": "pinned-worst-case", "required": false },
        { "path": "amharic-realize.toml", "role": "realizer-config", "required": false }
      ],
      "requiring_tests": [
        "pg-foma --test f3_interdigitation_gate",
        "pg-foma --test p6_gate_parity amharic_gated_subrules_and_tuple_counts_unregressed",
        "pg-foma --test five_language_backend_reports_gate amharic_backend_reports_are_complete"
      ]
    },
    {
      "logical_name": "aweti",
      "purpose": "Deep truncation chain with a 24-level derivation and many zero-width truncation mrules. The enumeration-explosion and compose-budget stress case; imported from fwdata/JSON rather than HC XML.",
      "files": [
        { "path": "aweti.json", "role": "grammar-snapshot", "required": false },
        {
          "path": "aweti-words.txt",
          "role": "corpus",
          "required": true,
          "word_list": {
            "line_ending": "CRLF",
            "skip_leading_lines": 0,
            "notes": "No header, but the phonology's phonemes/boundaryMarkers (samples/data/aweti.json) define no hyphen or space, so lines containing either (e.g. 'itywytu-put', 'waráju-puza', 'kaminuʼat-puza', 'ị- tyᴾ', 'awytyza ʼytoto') are guaranteed batch status SKIPPED. Line 147 is a stray 'µ' (micro sign) with no linguistic content; line 175 'wejmopáb' ends in a bare 'b', a phoneme this inventory does not define outside its ejective-marked forms. The slow-word tail is broad: a full-corpus oracle sample timed out on 61 of 196 non-skipped words. Sampling MUST pass both --word-timeout-ms and --threads=1. TunedSurface exceeds the current default closure-work envelope and the other two backends have typed coverage gaps, so normal selection has no path and writes no trusted FST. The legacy eager construction measured 3,093,412 composites; that scale evidence must not be mistaken for a safe retry recommendation."
          }
        },
        { "path": "aweti.fwdata", "role": "grammar-fwdata", "required": true }
      ],
      "requiring_tests": [
        "pg-foma --test p6_templated_morphotactics_gate",
        "pg-foma --test five_language_backend_reports_gate aweti_backend_reports_are_complete"
      ]
    },
    {
      "logical_name": "mbugwe",
      "purpose": "Real-language fwdata import through LibLCM and the fifth reference grammar for fail-closed backend selection, full-engine smoke coverage, and FST-propose feasibility evidence.",
      "files": [
        { "path": "mbugwe.fwdata", "role": "grammar-fwdata", "required": true },
        {
          "path": "mbugwe-words.txt",
          "role": "corpus",
          "required": true,
          "word_list": {
            "line_ending": "CRLF",
            "skip_leading_lines": 0,
            "notes": "No header found: a full-file sweep for digits, spaces, hyphens, and uppercase ASCII letters found none across all 1638 lines. This is not a claim that every line is a well-formed surface word. The grammar is slow in the full engine across a large minority of words: a 60-word sample at 10s/word timed out on 22, and a 200-word sample at 5s/word timed out on 100. Sampling MUST pass --word-timeout-ms. TunedSurface exceeds the current default closure-work envelope, TemplatedUnderlyingTokens has a nonregular-process coverage gap, and PlanComposed would omit required marker subtrees; normal selection therefore has no path and writes no trusted FST. The legacy eager run that stopped around 200,700 composites used an obsolete 200,000-entry envelope and is historical scale evidence, not the current refusal threshold."
          }
        }
      ],
      "requiring_tests": [
        "pg-foma --test mbugwe_corpus_smoke_gate mbugwe_grammar_compiles_and_oracle_parses_a_sample",
        "pg-foma --test five_language_backend_reports_gate mbugwe_backend_reports_are_complete"
      ]
    }
  ]
}
