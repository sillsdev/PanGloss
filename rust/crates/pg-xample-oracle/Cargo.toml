[package]
name = "pg-xample-oracle"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
# `identity::AnalysisIdentity` is the stable-key projection this crate reuses rather than
# re-deriving (CLAUDE.md "never re-derive a decision another module makes").
pg-parse.workspace = true
pg-grammar.workspace = true
unicode-normalization.workspace = true
serde.workspace = true
serde_json.workspace = true

# Opt in to [workspace.lints] (rustdoc::broken_intra_doc_links = deny).
[lints]
workspace = true
