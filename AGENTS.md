# Agent workflow

- For substantial PanGloss work, use the `agent-handoff` skill and delegate concrete, bounded research or implementation tasks to Luna as the normal path. Up to two Luna agents may receive `Workspace` access under the standing repository disclosure authorization.
- The primary agent must personally inspect every delegated diff and claim, resolve integration issues, and rerun the authoritative verification. Never treat a Luna result as trusted without review.
